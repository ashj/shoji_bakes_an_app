<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widget_ingredient_background"
    android:padding="@dimen/widget_margin">

    <TextView
        android:id="@+id/appwidget_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_margin="@dimen/widget_ingredient_margin"
        android:background="@color/widget_ingredient_background"
        android:contentDescription="@string/widget_name_ingredient_list"
        android:text="@string/widget_name_recipe_list"
        android:textColor="@color/widget_ingredient_textColor"
        android:textSize="@dimen/widget_ingredient_text_size" />

</RelativeLayout>